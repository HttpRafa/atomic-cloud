[workspace]
resolver = "2"
members = [
    # Common
    "common",

    # Controller
    "controller",

    # Drivers
    "drivers/pterodactyl",

    # Clients
    "cli",
    "clients/wrapper",
]

[profile.release]
lto = true
strip = true
