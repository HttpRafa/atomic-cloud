[workspace]
resolver = "2"
members = [
    # Common
    "common",

    # Controller
    "controller",

    # Drivers
    "plugins/pelican",
    "plugins/local",

    # Clients
    "cli",
    "clients/wrapper",
]

[workspace.metadata]
protocol-version = 7

[profile.release]
lto = true