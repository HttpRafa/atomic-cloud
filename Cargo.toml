[workspace]
resolver = "2"
members = [
    # Common
    "common",

    # Controller
    "controller",

    # Drivers
    "drivers/pterodactyl",

    # Clients
    "cli",
    "clients/wrapper",
]

[workspace.metadata]
protocol-version = 3

[profile.release]
lto = true
strip = true
