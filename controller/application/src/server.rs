pub struct Servers {

}

impl Servers {
    pub fn new() -> Self {
        Self {}
    }
    pub fn tick(&self) {
        // Tick server manager
        // Check if all server have send there heartbeats etc..
    }
}