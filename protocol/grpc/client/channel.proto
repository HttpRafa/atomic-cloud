syntax = "proto3";

option java_multiple_files = true;
option java_package = "io.atomic.cloud.grpc.client";

package client;

message Channel {
  message Msg {
    string channel = 1;
    bytes data = 2;
    uint64 timestamp = 3;   // timestamp (e.g. epoch time)
  }
}